import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'passwordGoblin',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
